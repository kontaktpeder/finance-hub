# Identity Core (Finance)

Finance trusts **Nexus** as the auth authority when `VITE_AUTH_SUPABASE_*` is set.

Full ops guide (JWT secret alignment, allowlist, dogfood checklist): Nexus repo `docs/IDENTITY_CORE.md`.

Module verify / API keys are unchanged.
